{
  "name": "shadow",
  "description": "Suite of utilities for managing user accounts, passwords, and login authentication, including login, passwd, su, useradd, and the shadow password file format.",
  "version": "4.17.1",
  "dependencies": [
    "autoconf",
    "automake",
    "libtool",
    "gettext"
  ],
  "download": [
    "curl -fL --retry 3 --retry-delay 2 -o shadow-4.17.1.tar.gz https://github.com/shadow-maint/shadow/releases/download/4.17.1/shadow-4.17.1.tar.gz",
    "tar -xf shadow-4.17.1.tar.gz"
  ],
  "build": [
    "cd shadow-4.17.1 && ./configure --prefix=\"$PREFIX\" --without-audit --without-selinux --without-acl --without-attr --disable-nls --without-libbsd",
    "cd shadow-4.17.1 && make $MAKEOPTS"
  ],
  "install": [
    "cd shadow-4.17.1 && make DESTDIR=\"$DESTDIR\" install"
  ],
  "strip": [
    "find \"$DESTDIR$PREFIX\" -type f -exec sh -c 'file \"$1\" | grep -q \"ELF\" && strip --strip-unneeded \"$1\"' _ {} \\; 2>/dev/null || true"
  ]
}
