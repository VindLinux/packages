{
  "name": "freetype",
  "description": "Font rendering library used to render fonts into bitmaps or pixmaps.",
  "version": "2.14.1",
  "dependencies": [],
  "download": [
    "curl -fL --retry 3 --retry-delay 2 -o freetype-2.14.1.tar.xz https://sourceforge.net/projects/freetype/files/freetype2/2.14.1/freetype-2.14.1.tar.xz/download",
    "tar -xf freetype-2.14.1.tar.xz"
  ],
  "build": [
    "cd freetype-2.14.1 && ./configure $XORG_CONFIG",
    "cd freetype-2.14.1 && make $MAKEOPTS"
  ],
  "install": [
    "cd freetype-2.14.1 && make DESTDIR=\"$DESTDIR\" install"
  ],
  "strip": [
    "find \"$DESTDIR$PREFIX\" -type f -exec sh -c 'file \"$1\" | grep -q \"ELF\" && strip --strip-unneeded \"$1\"' _ {} \\; 2>/dev/null || true"
  ]
}
