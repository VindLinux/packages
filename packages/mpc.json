{
  "name": "mpc",
  "description": "GNU Multiple Precision Complex Library, a required dependency for building GCC.",
  "version": "1.3.1",
  "dependencies": [
    "gmp",
    "mpfr"
  ],
  "download": [
    "curl -fL --retry 3 --retry-delay 2 -o mpc-1.3.1.tar.gz https://ftp.gnu.org/gnu/mpc/mpc-1.3.1.tar.gz",
    "tar -xf mpc-1.3.1.tar.gz"
  ],
  "build": [
    "cd mpc-1.3.1 && ./configure --prefix=\"$PREFIX\" --with-gmp=\"$PREFIX\" --with-mpfr=\"$PREFIX\"",
    "cd mpc-1.3.1 && make $MAKEOPTS"
  ],
  "install": [
    "cd mpc-1.3.1 && make DESTDIR=\"$DESTDIR\" install"
  ],
  "strip": [
    "find \"$DESTDIR$PREFIX\" -type f -exec sh -c 'file \"$1\" | grep -q \"ELF\" && strip --strip-unneeded \"$1\"' _ {} \\; 2>/dev/null || true"
  ]
}
