{
    "name": "mesa",
    "description": "Provides libraries and drivers for graphics APIs like OpenGL and Vulkan, including libGL, libEGL, libgbm, and Gallium3D/VA-API drivers.",
    "version": "25.3.5",
    "dependencies": [
        "xorg-libraries",
        "libdrm",
        "libelf",
        "mako",
        "pyyaml",
        "glslang",
        "rustc",
        "rust-bindgen",
        "libclc",
        "meson",
        "ninja",
        "wayland-protocols"
    ],
    "download": [
        "curl -fL --retry 3 --retry-delay 2 -o mesa-25.3.5.tar.xz https://mesa.freedesktop.org/archive/mesa-25.3.5.tar.xz",
        "tar -xf mesa-25.3.5.tar.xz"
    ],
    "build": [
        "meson setup mesa-25.3.5/build mesa-25.3.5 --prefix=\"$XORG_PREFIX\" --libdir=\"$XORG_PREFIX/lib\" --buildtype=release -D platforms=x11,wayland -D gallium-drivers=llvmpipe,radeonsi,r300,r600,nouveau,zink -D vulkan-drivers=amd -D valgrind=disabled -D video-codecs=all -D libunwind=disabled --wrap-mode=nodownload",
        "ninja -C mesa-25.3.5/build $MAKEOPTS"
    ],
    "install": [
        "DESTDIR=\"$DESTDIR\" ninja -C mesa-25.3.5/build install"
    ]
}
