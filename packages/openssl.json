{
  "name": "openssl",
  "description": "Management tools and cryptography library providing SSL/TLS protocol implementation and general-purpose cryptographic functions, used by OpenSSH, web browsers, and other software requiring encrypted communication.",
  "version": "3.5.2",
  "dependencies": [
    "perl",
    "zlib"
  ],
  "download": [
    "curl -fL --retry 3 --retry-delay 2 -o openssl-3.5.2.tar.gz https://github.com/openssl/openssl/releases/download/openssl-3.5.2/openssl-3.5.2.tar.gz",
    "tar -xf openssl-3.5.2.tar.gz"
  ],
  "build": [
    "cd openssl-3.5.2 && ./config --prefix=\"$PREFIX\" --openssldir=/etc/ssl --libdir=lib shared zlib-dynamic",
    "cd openssl-3.5.2 && make $MAKEOPTS"
  ],
  "install": [
    "cd openssl-3.5.2 && sed -i '/INSTALL_LIBS/s/libcrypto.a libssl.a//' Makefile",
    "cd openssl-3.5.2 && make DESTDIR=\"$DESTDIR\" MANSUFFIX=ssl install",
    "mv -v \"$DESTDIR$PREFIX/share/doc/openssl\" \"$DESTDIR$PREFIX/share/doc/openssl-3.5.2\""
  ],
  "strip": [
    "find \"$DESTDIR$PREFIX\" -type f -exec sh -c 'file \"$1\" | grep -q \"ELF\" && strip --strip-unneeded \"$1\"' _ {} \\; 2>/dev/null || true"
  ]
}
