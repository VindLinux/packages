{
  "name": "dhcpcd",
  "description": "Lightweight and portable DHCP client used to configure network interfaces automatically via DHCP for both IPv4 and IPv6.",
  "version": "10.5.0",
  "dependencies": [],
  "download": [
    "curl -fL --retry 3 --retry-delay 2 -o dhcpcd-10.5.0.tar.xz https://github.com/NetworkConfiguration/dhcpcd/releases/download/v10.5.0/dhcpcd-10.5.0.tar.xz",
    "tar -xf dhcpcd-10.5.0.tar.xz"
  ],
  "build": [
    "cd dhcpcd-10.5.0 && ./configure --prefix=\"$PREFIX\"",
    "cd dhcpcd-10.5.0 && make $MAKEOPTS"
  ],
  "install": [
    "cd dhcpcd-10.5.0 && make DESTDIR=\"$DESTDIR\" install"
  ],
  "strip": [
    "find \"$DESTDIR$PREFIX\" -type f -exec sh -c 'file \"$1\" | grep -q \"ELF\" && strip --strip-unneeded \"$1\"' _ {} \\; 2>/dev/null || true"
  ]
}
