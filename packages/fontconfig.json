{
  "name": "fontconfig",
  "description": "Library and utilities for configuring and customizing font access.",
  "version": "2.18.2",
  "dependencies": [
    "freetype",
    "expat",
    "gperf"
  ],
  "download": [
    "curl -fL --retry 3 --retry-delay 2 -o fontconfig-2.18.2.tar.xz https://gitlab.freedesktop.org/api/v4/projects/890/packages/generic/fontconfig/2.18.2/fontconfig-2.18.2.tar.xz",
    "tar -xf fontconfig-2.18.2.tar.xz"
  ],
  "build": [
    "cd fontconfig-2.18.2 && ./configure $XORG_CONFIG --disable-docs",
    "cd fontconfig-2.18.2 && make $MAKEOPTS"
  ],
  "install": [
    "cd fontconfig-2.18.2 && make DESTDIR=\"$DESTDIR\" install"
  ],
  "strip": [
    "find \"$DESTDIR$PREFIX\" -type f -exec sh -c 'file \"$1\" | grep -q \"ELF\" && strip --strip-unneeded \"$1\"' _ {} \\; 2>/dev/null || true"
  ]
}
