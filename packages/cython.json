{
  "name": "cython",
  "description": "A compiler for writing C extensions for the Python language.",
  "version": "3.2.4",
  "dependencies": [],
  "download": [
    "curl -fL --retry 3 --retry-delay 2 -o cython-3.2.4.tar.gz https://github.com/cython/cython/releases/download/3.2.4/cython-3.2.4.tar.gz",
    "tar -xf cython-3.2.4.tar.gz"
  ],
  "build": [
    "cd cython-3.2.4 && pip3 wheel -w dist --no-build-isolation --no-deps --no-cache-dir \"$PWD\""
  ],
  "install": [
    "cd cython-3.2.4 && pip3 install --break-system-packages --no-index --find-links dist --no-user --root=\"$DESTDIR\" --prefix=\"$PREFIX\" Cython"
  ],
  "strip": [
    "find \"$DESTDIR$PREFIX\" -type f -exec sh -c 'file \"$1\" | grep -q \"ELF\" && strip --strip-unneeded \"$1\"' _ {} \\; 2>/dev/null || true"
  ]
}
