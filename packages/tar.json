{
  "name": "tar",
  "description": "Standard archiving utility for creating and extracting tar archives, supporting multiple compression formats.",
  "version": "1.35",
  "dependencies": [],
  "download": [
    "curl -fL --retry 3 --retry-delay 2 -o tar-1.35.tar.gz https://ftp.gnu.org/gnu/tar/tar-1.35.tar.gz",
    "tar -xf tar-1.35.tar.gz"
  ],
  "build": [
    "cd tar-1.35 && FORCE_UNSAFE_CONFIGURE=1 ./configure --prefix=\"$PREFIX\"",
    "cd tar-1.35 && make $MAKEOPTS"
  ],
  "install": [
    "cd tar-1.35 && make DESTDIR=\"$DESTDIR\" install"
  ],
  "strip": [
    "find \"$DESTDIR$PREFIX\" -type f -exec sh -c 'file \"$1\" | grep -q \"ELF\" && strip --strip-unneeded \"$1\"' _ {} \\; 2>/dev/null || true"
  ]
}
