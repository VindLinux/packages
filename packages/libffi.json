{
  "name": "libffi",
  "description": "Portable, high-level foreign function interface (FFI) library that allows code written in one language, such as an interpreter, to call functions in compiled code written in another language like C or C++.",
  "version": "3.5.2",
  "dependencies": [],
  "download": [
    "curl -fL --retry 3 --retry-delay 2 -o libffi-3.5.2.tar.gz https://github.com/libffi/libffi/releases/download/v3.5.2/libffi-3.5.2.tar.gz",
    "tar -xf libffi-3.5.2.tar.gz"
  ],
  "build": [
    "cd libffi-3.5.2 && ./configure --prefix=\"$PREFIX\" --disable-static --with-gcc-arch=native",
    "cd libffi-3.5.2 && make $MAKEOPTS"
  ],
  "install": [
    "cd libffi-3.5.2 && make DESTDIR=\"$DESTDIR\" install"
  ],
  "strip": [
    "find \"$DESTDIR$PREFIX\" -type f -exec sh -c 'file \"$1\" | grep -q \"ELF\" && strip --strip-unneeded \"$1\"' _ {} \\; 2>/dev/null || true"
  ]
}
