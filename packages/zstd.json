{
  "name": "zstd",
  "description": "Zstandard, a fast lossless compression library and command-line utility offering zlib-level and better compression ratios with very fast decompression.",
  "version": "1.5.7",
  "dependencies": [],
  "download": [
    "curl -fL --retry 3 --retry-delay 2 -o zstd-1.5.7.tar.gz https://github.com/facebook/zstd/releases/download/v1.5.7/zstd-1.5.7.tar.gz",
    "tar -xf zstd-1.5.7.tar.gz"
  ],
  "build": [
    "cd zstd-1.5.7 && make $MAKEOPTS"
  ],
  "install": [
    "cd zstd-1.5.7 && make DESTDIR=\"$DESTDIR\" PREFIX=\"$PREFIX\" install"
  ],
  "strip": [
    "find \"$DESTDIR$PREFIX\" -type f -exec sh -c 'file \"$1\" | grep -q \"ELF\" && strip --strip-unneeded \"$1\"' _ {} \\; 2>/dev/null || true"
  ]
}
