{
  "name": "mako",
  "description": "A hyperfast and lightweight templating module for Python.",
  "version": "1.3.10",
  "dependencies": [
    "markupsafe"
  ],
  "download": [
    "curl -fL --retry 3 --retry-delay 2 -o mako-1.3.10.tar.gz https://files.pythonhosted.org/packages/source/M/Mako/mako-1.3.10.tar.gz",
    "tar -xf mako-1.3.10.tar.gz"
  ],
  "build": [
    "cd mako-1.3.10 && pip3 wheel -w dist --no-build-isolation --no-deps --no-cache-dir \"$PWD\""
  ],
  "install": [
    "cd mako-1.3.10 && pip3 install --break-system-packages --no-index --find-links dist --no-user --root=\"$DESTDIR\" --prefix=\"$PREFIX\" Mako"
  ],
  "strip": [
    "find \"$DESTDIR$PREFIX\" -type f -exec sh -c 'file \"$1\" | grep -q \"ELF\" && strip --strip-unneeded \"$1\"' _ {} \\; 2>/dev/null || true"
  ]
}
