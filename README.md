# Vind Linux Packages

Package recipes for [Vind Linux](https://github.com/VindLinux/vindlinux), managed by [Lambda](https://github.com/VindLinux/lambda-manager).

## Structure

Each package is defined by a JSON recipe under `packages/`.

```text
packages/
├── busybox.json
├── curl.json
├── llvm.json
└── ...
