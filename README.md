# GroveOS Packages

Package recipes for [GroveOS](https://github.com/GroveOS/groveos), managed by [Lambda](https://github.com/GroveOS/lambda-manager).

## Structure

Each package is defined by a JSON recipe under `packages/`.

```text
packages/
├── busybox.json
├── curl.json
├── llvm.json
└── ...
